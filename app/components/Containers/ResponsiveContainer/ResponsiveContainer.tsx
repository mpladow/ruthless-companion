import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ResponsiveContainer = () => {
  return (
	 <View>
		<Text>ResponsiveContainer</Text>
	 </View>
  )
}

export default ResponsiveContainer

const styles = StyleSheet.create({})