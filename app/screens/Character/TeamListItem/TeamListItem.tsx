import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TeamListItem = () => {
	return (
		<View>
			<Text>TeamListItem</Text>
		</View>
	)
}

export default TeamListItem

const styles = StyleSheet.create({})